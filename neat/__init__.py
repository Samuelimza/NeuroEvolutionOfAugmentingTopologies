'''
These imports add modules to the namespace of the package
The relative imports in other modules (i.e ".Something") only work when those importing
modules are part of the package
'''
from neat import Main, population, speciate, config, activationFunctions, neuralNetwork, genome, gene
